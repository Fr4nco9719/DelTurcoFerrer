version: "3.8"

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-reentrega
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: delturcoferrer_2
    ports:
      - "3308:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
